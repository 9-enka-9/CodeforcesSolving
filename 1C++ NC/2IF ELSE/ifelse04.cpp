#include<bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	const double pi=3.14;
	int r;
	cin>>r;
	double cvi=2*pi*r, dt=r*r*pi;
	cout<<fixed<<setprecision(4)<<cvi<<" "<<dt;
}
